- clean: ['~']

- defaults:
    link:
      create: true
      force: true

- link:
    ~/.dotly: os/linux/.dotly
    ~/.config/alacritty/alacritty.toml: os/linux/alacritty.toml
    ~/.config/nvim: editors/nvim
    ~/.gitconfig: git/.gitconfig
